#include "main.h"

/**
 * _isupper - Checks for lowercase character
 * @c: The function parameter
 * Return: 1 for uppercase or return 0 if anything else
 */

int _isupper(int c)

{
if (c >= 65 && c <= 90)
{
return (1);
}
return (0);
}
